filter:
    excluded_paths: [test/*]
checks:
    javascript: true
build:
    nodes:
        analysis:
            tests:
                override:
                    - js-scrutinizer-run
                    coverage:
                    file: coverage
                    format: clover