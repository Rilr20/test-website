filter:
  excluded_paths: [test/*]
checks:
  javascript: true
build:
  nodes:
    analysis:
      tests:
        override:
          - js-scrutinizer-run
      testss:
        coverage:
          file: coverage/clover.xml
          format: clover
