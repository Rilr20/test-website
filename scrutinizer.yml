filter:
    excluded_paths: [test/s*]
checks:
    javascript: true
build:
    nodes:
        analysis:
            tests:
                override:
                    - js-scrutinizer-run
    tests:
        override:
            -
                command: "npm run test"
                coverage:
                    file: coverage/clover.xml
                    format: "clover"