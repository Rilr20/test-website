filter:
  excluded_paths: [test/*, node_modules/*]
checks:
  javascript: true
build:
  nodes:
    analysis:
      tests:
        override:
          -
            command: "npm -v"
            coverage:
              file: coverage/clover.xml
              format: clover
